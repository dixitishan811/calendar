feat: update compile and target SDK versions to 36 and update dependencies

This commit updates the compile and target SDK versions to 36 in `libs.versions.toml`, and also updates several library dependencies. Key changes include:

-   Updating the `agp` (Android Gradle Plugin) version in `libs.versions.toml` from 8.9.1 to 8.9.2.
-   Updating the `android-compileSdk` from 35 to 36.
-   Updating the `android-targetSdk` from 35 to 36.
- Updating Koin version from 4.1.0-Beta7 to 4.1.0-Beta8.
- Updating Navigation version from 2.9.0-alpha16 to 2.9.0-alpha17.
- Adding import `org.jetbrains.kotlin.gradle.tasks.KotlinCompilationTask` to `build.gradle.kts`.
